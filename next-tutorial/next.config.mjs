/** @type {import('next').NextConfig} */
const nextConfig = {

    /*
    images: {
        remotePattern: [
            {
                protocol: 'https',
                hostname: 's3.amazonaws.com', 
                port: '',
                pathname: '/my-bucket/**', 
            },
        ],
    },              */
};

export default nextConfig;
